package demo;
import java.lang.*; //主要是含语言方面的，包由解释程序自动加载，不需要显示说明

import java.io.*; //主要含有与输入/输出相关的类，这些类提供了对不同的输入和输出设备读写数据的支持，这些输入和输出设备包括键盘、显示器、打印机、磁盘文件等。

import java.util.*; //包括许多具有特定功能的类，有日期、向量、哈希表、堆栈等，其中 Date类支持与时间有关的操作。

//import java.swing.*;
import java.awt.*; //提供了创建图形用户界面元素的类。通过这些元素，编程者可以控制所写的 Applet 或 Application 的外观界面。包中包含了窗口、对话框、菜单等类。

import java.net.*; // 含有与网络操作相关的类，如 TCP Scokets、URL 等工具。

import java.applet.*; //含有控制 HTML 文档格式、应用程序中的声音等资源的类，其中 Applet类是用来创建包含于 HTML 的 Applet 必不可少的类。

import java.beans.*; // 定义了应用程序编程接口（API），Java Beans 是 Java 应用程序环境的中性平台组件结构。

public class Bao {
	public static void main(String[] args) {
		int a;
		System.out.println("随机数为：");
		for(int i = 1; i <= 10; i++) {
			a = (int)( (100 - 10 + 1) * Math.random()+10 );
		}
	}
}
